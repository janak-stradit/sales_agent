# ── DATABASE ──────────────────────────────────────────────────────
DATABASE_URL=postgresql://salesai:salesai_dev_pass@localhost:5432/sales_ai

# ── REDIS (Celery Broker + Cache) ─────────────────────────────────
REDIS_URL=redis://localhost:6379/0

# ── MONID.AI MCP (Primary Data Layer) ─────────────────────────────
MONID_API_KEY=your-monid-api-key-here

# ── AI APIs & Vector Store ────────────────────────────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
CHROMA_PERSIST_DIR=chroma_db
CHROMA_COLLECTION_NAME=sales_ai_embeddings
EMBEDDING_PROVIDER=local

# ── EMAIL (Phase 2+) ──────────────────────────────────────────────
SENDGRID_API_KEY=SG-...
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587

# ── APP ───────────────────────────────────────────────────────────
SECRET_KEY=your-super-secret-key-here
ENVIRONMENT=development
